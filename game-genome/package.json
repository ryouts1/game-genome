{
  "name": "game-genome",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "JSON-defined board game framework with a shared UI, rule engine, and minimax/MCTS AI.",
  "scripts": {
    "start": "node scripts/dev-server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}

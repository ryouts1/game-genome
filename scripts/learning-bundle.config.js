export const LEARNING_BUNDLE_CONFIG = [
  {
    gameId: 'tic-tac-toe',
    outputFilename: 'tic-tac-toe.book.json',
    sourceLabel: 'self-play bundle',
    teacherController: 'minimax',
    teacherOptions: { depth: 4, candidateLimit: 5 },
    episodes: 320,
    epsilon: 0.18,
    hardMoveLimit: 12,
    seed: 11,
    minimumVisits: 6,
    minimumConfidence: 0.18,
    minStateVisits: 2,
    minActionVisits: 2,
  },
  {
    gameId: 'connect-four',
    outputFilename: 'connect-four.book.json',
    sourceLabel: 'self-play bundle',
    teacherController: 'mcts',
    teacherOptions: { iterations: 120, candidateLimit: 5 },
    episodes: 18,
    epsilon: 0.35,
    hardMoveLimit: 52,
    seed: 23,
    minimumVisits: 3,
    minimumConfidence: 0.08,
    minStateVisits: 1,
    minActionVisits: 1,
  },
  {
    gameId: 'lane-breakthrough',
    outputFilename: 'lane-breakthrough.book.json',
    sourceLabel: 'self-play bundle',
    teacherController: 'minimax',
    teacherOptions: { depth: 3, candidateLimit: 5 },
    episodes: 220,
    epsilon: 0.16,
    hardMoveLimit: 60,
    seed: 37,
    minimumVisits: 6,
    minimumConfidence: 0.12,
    minStateVisits: 2,
    minActionVisits: 2,
  },
  {
    gameId: 'knight-skirmish',
    outputFilename: 'knight-skirmish.book.json',
    sourceLabel: 'self-play bundle',
    teacherController: 'minimax',
    teacherOptions: { depth: 3, candidateLimit: 5 },
    episodes: 40,
    epsilon: 0.18,
    hardMoveLimit: 70,
    seed: 41,
    minimumVisits: 4,
    minimumConfidence: 0.1,
    minStateVisits: 2,
    minActionVisits: 2,
  },
  {
    gameId: 'royal-duel',
    outputFilename: 'royal-duel.book.json',
    sourceLabel: 'self-play bundle',
    teacherController: 'mcts',
    teacherOptions: { iterations: 140, candidateLimit: 5 },
    episodes: 60,
    epsilon: 0.16,
    hardMoveLimit: 80,
    seed: 53,
    minimumVisits: 4,
    minimumConfidence: 0.1,
    minStateVisits: 2,
    minActionVisits: 2,
  },
  {
    gameId: 'relay-outpost',
    outputFilename: 'relay-outpost.book.json',
    sourceLabel: 'self-play bundle',
    teacherController: 'mcts',
    teacherOptions: { iterations: 140, candidateLimit: 5 },
    episodes: 20,
    epsilon: 0.22,
    hardMoveLimit: 80,
    seed: 67,
    minimumVisits: 3,
    minimumConfidence: 0.08,
    minStateVisits: 1,
    minActionVisits: 1,
  },
];
